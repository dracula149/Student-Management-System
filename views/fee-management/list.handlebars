<table class="table table-striped table-bordered mt-5">
        <thead class="thead-dark">
            <tr>
                <th scope="col">Rollno</th>
                <th scope="col">Name</th>
                <th scope="col">Class</th>
                <th scope="col">Department</th>
                <th scope="col">Amount paid</th>
                <th scope="col">Amount Due</th>
                <th scope="col" class="dropdown-hide">Action</th>
            </tr>
        </thead>
        <tbody>
            {{#each users}}
            <tr>
                <td>{{studentRoll}}</td>
                <td>{{studentName}}</td>
                <td>{{class}}</td>
                <td>{{department}}</td>
                <td>{{amountPaid}}</td>
                <td>{{amountDue}}</td>
                <td class="dropdown-hide">
                    <div class="dropdown dropdown-hide" id="customized-dropdown">
                        <button class="btn btn-secondary" type="button" data-toggle="dropdown">
                            <i class="fa fa-ellipsis-v"></i>
                        </button>
                        <div class="dropdown-menu dropdown-menu-right animated fadeIn">
                            {{!-- <a class="dropdown-item select-item" href="" onclick="findUser('/fee-management/edit/{{id}}');">Edit</a> --}}
                            <a id="delete-item" class="dropdown-item" onclick="deleteItem('/fee-management/{{id}}');" href="#">Delete</a>
                        </div>
                    </div>
                </td>
            </tr>
            {{/each}}
        </tbody>
    </table>
